apiVersion: 1

providers:
  - name: 'CY Weather Dashboards'
    orgId: 1
    folder: 'CY Weather'
    folderUid: 'cy-weather'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
